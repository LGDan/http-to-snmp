{
  "name": "http-to-snmp",
  "version": "1.0.0",
  "description": "Simple HTTP API to query SNMP agents.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LGDan",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "net-snmp": "^3.8.2"
  }
}
